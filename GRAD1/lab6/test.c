#include <stdio.h>
#include <stdio.h>
int func(int *a, int b)
{
    - -(*a);
    if (*a * b)
        return b;
    else
        return 0;
}
int f(int a)
{
    if (a <= 0)
        return 2;
    else
        return f(a - 1) * f(a - 2);
}

int main(void)
{
    char c;
    c = getchar() ;
    while (c != getchar())
        c = getchar();
}
